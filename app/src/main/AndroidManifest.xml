<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- QUYỀN TRUY CẬP (Permissions) -->
    <!-- Internet & Mạng: Để tải bản đồ OSM -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Vị trí: Để hiển thị vị trí người dùng trên bản đồ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Truy vấn các app chỉ đường (Google Maps, etc) -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportBookingApp"
        tools:targetApi="31">

        <!-- 1. Màn hình chính -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Màn hình Chi tiết sân -->
        <activity
            android:name=".activity.CourtDetailActivity"
            android:exported="false" />

        <!-- 3. Màn hình Chọn ngày & Giờ -->
        <activity
            android:name=".activity.BookingActivity"
            android:exported="false" />

        <!-- 4. Màn hình Xác nhận Đặt sân -->
        <activity
            android:name=".activity.ConfirmBookingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- 5. Màn hình Chi tiết booking (nếu còn dùng) -->
        <activity
            android:name=".activity.BookingDetailActivity"
            android:exported="false" />

    </application>

</manifest>